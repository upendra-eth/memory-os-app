import { redirect } from 'next/navigation'

// Merged into the unified analytics hub at /dashboard (Nutrition & Fitness charts).
export default function NutritionFitnessPage() {
  redirect('/dashboard')
}
